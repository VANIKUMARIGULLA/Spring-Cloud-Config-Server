server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/VANIKUMARIGULLA/Spring-Cloud-Config-Server
          default-label: main
           clone-on-start: true
